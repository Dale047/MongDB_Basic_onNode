/*
	equals(doc)
		判断两个对象是否相等
	id
		根据查询到对象找到_id
	get
		获取到指定属性值
	set
		设置当前属性值
	update
		修改当前属性值
	save
		保存当前添加的对象
	remove
		移除当前修改的数据
	isNew
		是否有新的文档，说白了就是有没有存入数据库
	isInit
		当前属性有没有初始化
	toJSON()
		转换成JSON对象

	toObject()
		将document对象转换成一个普通的js对象
		（不可以操作数据的那种，最初始的js对象，仅仅能保存数据）
*/